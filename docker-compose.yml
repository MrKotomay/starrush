name: starrush-vds

x-app-common: &app-common
  build:
    context: .
    dockerfile: Dockerfile
    args:
      NEXT_PUBLIC_WS_URL: ${DOCKER_NEXT_PUBLIC_WS_URL:-}
      NEXT_PUBLIC_GATEWAY_PORT: ${DOCKER_NEXT_PUBLIC_GATEWAY_PORT:-8081}
  env_file:
    - .env
  environment:
    NODE_ENV: production
    DATABASE_URL: ${DOCKER_DATABASE_URL:?DOCKER_DATABASE_URL is required for docker-compose.yml (DBaaS mode)}
    REDIS_URL: ${DOCKER_REDIS_URL:?DOCKER_REDIS_URL is required for docker-compose.yml (DBaaS mode)}
  restart: unless-stopped

services:
  migrate:
    <<: *app-common
    command: ["npx", "prisma", "migrate", "deploy"]
    restart: "no"

  app:
    <<: *app-common
    command: ["npm", "run", "start"]
    expose:
      - "3000"

  gateway:
    <<: *app-common
    command: ["npm", "run", "gateway"]
    environment:
      NODE_ENV: production
      DATABASE_URL: ${DOCKER_DATABASE_URL:?DOCKER_DATABASE_URL is required for docker-compose.yml (DBaaS mode)}
      REDIS_URL: ${DOCKER_REDIS_URL:?DOCKER_REDIS_URL is required for docker-compose.yml (DBaaS mode)}
      GATEWAY_PORT: 8081
    expose:
      - "8081"

  worker:
    <<: *app-common
    command: ["npm", "run", "worker:round"]

  edge:
    image: caddy:2.10-alpine
    restart: unless-stopped
    environment:
      APP_DOMAIN: ${APP_DOMAIN:-localhost}
      LETSENCRYPT_EMAIL: ${LETSENCRYPT_EMAIL:-admin@example.com}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infra/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      app:
        condition: service_started
      gateway:
        condition: service_started

volumes:
  caddy_data:
    driver: local
  caddy_config:
    driver: local
